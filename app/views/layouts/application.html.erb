<!DOCTYPE html>
<html>
  <head>
<title>Wake and Save</title>

    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <!-- temporary logout link -->
    <%= link_to 'Log Out', destroy_user_session_path %>


    <% flash.each do |key, value| %>
      <% if key == "notice" %>
        <div class="alert alert-success"><%= value %><button class="close" data-dismiss="alert">×</button></div>
      <% elsif key == "alert" %>
        <div class="alert alert-danger"><%= value %><button class="close" data-dismiss="alert">×</button></div>
      <% else %>
        <div class="alert alert-<%= key %>"><%= value %><button class="close" data-dismiss="alert">×</button></div>
      <% end %>
    <% end %>

    <%= yield %>

    <% if Rails.env.development? %>
      <%= console %>
    <% end %>
  </body>
</html>
